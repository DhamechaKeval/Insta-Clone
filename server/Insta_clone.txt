//featurs 
=> Authentication 
    => Register
    => Login
    => Logout (token blacklisting)

=> user
    => following 
    => followers

=> Post
    => Create Post
    => Like Post
    => Save Post
    => user can see the feed

// User Schema 
{
    username : String , unique
    email : String , unique
    password : String , unique
    bio : String
    profileImage : String , default 
}

// Post Schema
{
    caption : String , default:""
    imgUrl : String
    user : UserId , ref: User
    createdAt
}

// Follow Schema (edge collection)
{
    follower : String
    followee : String
    createdAt
}

// Like Schema (edge collection)
{
    postId : ref : "posts"
    user : String
    createdAt
}